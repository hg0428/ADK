funct hi(String:a, Number:b=0) while b<a.length if a[b] != " " {
	b++
	ouput("This should work.", a[b])
}

static funct output(...args) {
	output("OUTPUT:", ...args)
}
hi(a="hello", b=0) // specifing args

